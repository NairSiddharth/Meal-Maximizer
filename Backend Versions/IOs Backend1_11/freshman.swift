import Foundation

Button(action: {
    preset(100,300)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(130,300)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(165,300)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(200,300)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(240,300)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(100,400)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(130,400)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(165,400)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(200,400)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(240,400)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})

Button(action: {
    preset(100,500)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(130,500)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(165,500)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(200,500)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(240,500)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
Button(action: {
    preset(0,1350)
}, label {
    // How the button looks like
     Text("Enter")
    .padding()
    .foregroundColor(.white)
    .backgroundColor(red: 60/255,green: 0/255,blue: 0/255,alpha: 1) //Maybe needs UIColor(___)
    .background(RoundedRectangle(cornerRadius: 8).stroke(red: 60/255,green: 0/255,blue: 0/255,alpha: 1, lineWidth: 2))
    // UIColor may also be needed here as well per https://stackoverflow.com/questions/24427284/change-button-background-color-using-swift-language
    .cornerRadius(8) // choose this or line above as one creates border around button, other just has rounded rectangle button
    // How the button looks like
})
