import Foundation

Button(action: {
    preset(45,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(100,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(130,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(165,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(200,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(240,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(45,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(100,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(130,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(165,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(200,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(240,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(45,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(100,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(130,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(165,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(200,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(240,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(0,1350)
}) {
    // How the button looks like
}
