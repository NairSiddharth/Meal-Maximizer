import Foundation


Button(action: {
    preset(165,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(200,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(240,300)
}) {
    // How the button looks like
}
Button(action: {
    preset(165,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(200,400)
}) {
    // How the button looks like
}
Button(action: {
    preset(240,400)
}) {
    // How the button looks like
}

Button(action: {
    preset(165,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(200,500)
}) {
    // How the button looks like
}
Button(action: {
    preset(240,500)
}) {
    // How the button looks like
}
