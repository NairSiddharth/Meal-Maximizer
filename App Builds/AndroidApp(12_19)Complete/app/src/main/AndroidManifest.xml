<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.release">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mmt_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mmt_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Count"></activity>
        <activity android:name=".Corp" />
        <activity android:name=".Upper" />
        <activity android:name=".freshman" />
        <activity android:name=".presets" />
        <activity android:name=".Custom" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>